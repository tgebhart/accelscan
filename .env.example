# Copy to ~/.config/accelscan.env (or ./.env) and fill in. SLURM scripts do:
#   set -a; source $HOME/.config/accelscan.env; set +a
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
ACCELSCAN_S3_ENDPOINT=https://s3.msi.umn.edu
S2_API_KEY=
