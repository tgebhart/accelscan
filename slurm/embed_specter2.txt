#!/bin/bash -l
#SBATCH --time=2:00:00
#SBATCH --signal=SIGUSR1@120
#SBATCH -p preempt-gpu
#SBATCH -A funkr
#SBATCH --nodes=1
#SBATCH --ntasks=1
#SBATCH --cpus-per-task=8
#SBATCH --mem=100gb
#SBATCH --gres=gpu:1
#SBATCH --requeue
#SBATCH --mail-type=END,FAIL
#SBATCH --mail-user=gebhart@umn.edu
export PYTHONUNBUFFERED=TRUE
export OMP_NUM_THREADS=4
export HF_HOME=$MSIPROJECT/gebhart/hf_cache
cd $MSIPROJECT/gebhart/projects/rfunklab/accelscan
set -a; source .env; set +a
module load cuda/12.0
source activate accelscan

# SPECTER2 embeddings of accelerator-paper abstracts (one GPU; ~minutes).
# Model loads once; chunks are .done-restartable, so a plain resubmit resumes.
python -m accelscan.embed
